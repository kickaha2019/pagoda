<%= erb :header, :locals => {:page => 'tables'} %>

  <table class="tables">
    <tr><th>Table</th><th>Records</th></tr>
    <%=
      html = []
      ['alias', 'aspect', 'bind', 'company', 'company_alias', 'game', 'link', 'old_links'].each do |name|
        html << "<tr><td>#{name}</td><td>#{$pagoda.count(name)}</td></tr>"
      end
      html.join( "\n")
    %>
  </table>

</body>
</html>